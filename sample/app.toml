# 应用主配置（TOML）—— 注释与键顺序在保存后会被原样保留（toml_edit 无损）
title = "示例应用"
version = "1.2.0"

[server]
host = "0.0.0.0"
port = 8000
workers = 4

[database]
url = "postgres://user:pass@localhost:5432/app"
pool_size = 10
timeout_seconds = 30

[logging]
level = "info"
json_output = false
